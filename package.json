{
  "name": "marp-template",
  "version": "1.1.0",
  "private": true,
  "description": "Marp theme reproducing the Bristol Flight Lab PowerPoint template",
  "license": "UNLICENSED",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BristolFlightLab/flightlab-marp-template.git"
  },
  "files": [
    "themes/",
    "src/",
    "assets/",
    "BRAND.md"
  ],
  "scripts": {
    "build:theme": "node scripts/build-theme.mjs",
    "brand": "sh scripts/sync-brand.sh",
    "build:files": "node -e \"require('fs').cpSync('example','dist',{recursive:true,filter:f=>!f.endsWith('.md')})\"",
    "build": "npm run build:theme && npm run build:files && marp example/slides.md -o dist/slides.html",
    "pdf": "npm run build:theme && marp example/slides.md -o dist/slides.pdf",
    "pptx": "npm run build:theme && marp example/slides.md -o dist/slides.pptx",
    "png": "npm run build:theme && marp example/slides.md --images png -o dist/slides.png",
    "watch": "npm run build:theme && npm run build:files && marp -w example/slides.md -o dist/slides.html",
    "serve": "npm run build:theme && marp -s ."
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.5.1"
  }
}
